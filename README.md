# service-end-to-end

Minimal setup for learning TypeScript with Node.js (ESM).

## Scripts

- `npm run dev` → run with hot reload (tsx)
- `npm run typecheck` → static type checking
- `npm run build` → compile to dist
- `npm run start` → run compiled code

## Stack

- Node 24
- TypeScript
- tsx
